meta {
  name: Campaign Runs
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/v1/crm/campaigns/{{campaign_id}}/runs/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Tenant-ID: {{tenant_id}}
  X-Store-ID: {{store_id}}
}
