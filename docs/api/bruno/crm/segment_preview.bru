meta {
  name: Segment Preview
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/crm/segments/{{segment_id}}/preview/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Tenant-ID: {{tenant_id}}
  X-Store-ID: {{store_id}}
}
