meta {
  name: Launch Campaign
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/v1/crm/campaigns/{{campaign_id}}/launch/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Tenant-ID: {{tenant_id}}
  X-Store-ID: {{store_id}}
}
