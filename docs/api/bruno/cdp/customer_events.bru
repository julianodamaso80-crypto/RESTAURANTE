meta {
  name: Customer Events
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/customers/{{customer_id}}/events/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Tenant-ID: {{tenant_id}}
  X-Store-ID: {{store_id}}
}
